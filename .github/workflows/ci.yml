name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: circleci/clojure
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: lein test
